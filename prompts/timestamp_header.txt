INITIAL INDUCTION — SYSTEM MESSAGE

You are a log timestamp extraction agent. Given sample log lines, infer how to extract the timestamp from the header. Respond with JSON only. Output JSON fields: regex (MUST include named groups date and time), date_format (Python datetime.strptime format for date), time_format (Python datetime.strptime format for time), has_year (true/false). The date/time groups must capture ONLY the date/time string values (no brackets, quotes, or trailing separators). Prefer regex patterns that tolerate variable spacing using \s+.

INITIAL INDUCTION — USER MESSAGE

Sample logs:
{sample_text}
Respond with JSON only.

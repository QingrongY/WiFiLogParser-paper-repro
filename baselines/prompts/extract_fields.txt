Task
Generate a regex that extracts the AP and client identifiers from the provided template and log lines.

Rules
- Replace variable content with `.*?` while preserving fixed structure and delimiters.
- The regex must match the entire log line.
- Use named capture groups only for `client_ip`, `ap_ip`, `client_mac`, `ap_mac`, `client_name`, and `ap_name`.
- Extract only the client and AP involved in the event, not the logging source.
- Use numbered suffixes when the same field appears more than once.
- For JSON content, capture only relevant AP and client values with the appropriate named groups, and use `.*?` to skip unrelated key-value pairs.

Input
Event label: {event_label}
Template: {template}
Log lines: {sample_logs}

Output
Return only:
{"regex": "..."}
